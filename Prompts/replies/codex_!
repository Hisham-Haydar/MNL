Simple state of play — the revision touched F3's *outputs*, so F4 needs three small patches to point at them. Here's the canonical set:

| Prompt | Status | Fire when |

|---|---|---|

| F3 (original) | **Dead** — superseded | never (stop it if still grinding; salvage IS outputs only if they match the dual-stem schema) |

| **F3-R** | **Live** — the one to run now | now |

| **F3-B** | Pre-defined, conditional | only on F3-R Task-4 verdict `LICENSED` (V_dir branch; parallel, not on F4's path) |

| F4 (as pasted) | **Stale in 3 places** — superseded by F4-R below | never |

| **F4-R** | **Live** — the measures prompt | F3-R report line `READY FOR F4: yes` |

What changed in F4 → F4-R: the precondition now points at F3-R's actual file (`singles_ViIS_dualstem_v1.parquet`, column `V_i_IS_staged`) instead of the old filename; the Task-1 consistency gate names that column; and the provenance block records the consumed parquet's content hash so the F3-R↔F4 tie is verifiable. **Nothing substantive changed** — F4 only ever consumes V_i^IS, so the Codex revision (which was all about V_dir gates and pricing scalability) doesn't touch the measures logic.

**F4-R** (supersedes F4 — Claude Code · Opus · high · thinking on · MNL repo):

```

FAST-LANE F4-R (supersedes F4): MEASURE-FAMILY REFERENCE SETS + EQUIVALENT-INCOME

INVERSION (SINGLES). Governing contract: JMP_measure_mapping_memo_v1 (signed).

Bounded build + run. NO inequality statistics of any kind (no Gini, no variance,

no percentile spreads of Omega - F5 computes those as its own gated increment).

No decomposition, no couples measures, no promotion, no edits to estimation

engines/specs/data.

PRECONDITION

The F3-R report ends "READY FOR F4: yes". Consume

outputs/welfare/fastlane/singles_ViIS_dualstem_v1.parquet as the SOLE V_actual

source: V_actual = the V_i_IS_staged column (staged stem, certified theta-hat).

Verify the parquet's recorded theta-hash + spec-hash against

theta_hat_realdata_901_v1.csv and the certified spec, and record the parquet's

content hash for provenance, before anything else; STOP on mismatch.

CONTEXT

Conventions locked in the contract: stem = fr_p3a_bpool_engine_ready_staged_threeB1

at certified theta-hat; unit = household, singles only; Omega units = real-2016 EUR

consumption levels (W4/W1/W6) or shift (W3, ~0); utility is u(c,l) only - occupation

and wage never enter preferences, so every reference side below is consumption-direct

and needs NO wage draw, NO occupation assignment, NO EUROMOD pricing.

The implemented, Stage-1-validated solver welfare_core.w3_inversion (zero-recovery

phi0=0.0, monotone, bracketed, converged for all HH) is the pattern: EXTEND it,

do not rebuild it.

TASK 0 - SOLVER EXTENSION + SYNTHETIC UNIT TESTS.

Add transform modes alongside the existing shift(w) [c_j + w]:

  replace(w): consumption := w at designated nodes;

  single_node(w): reference = one node (home) at consumption w.

Bracket per contract: w in [DCM_MIN_POSITIVE=1.0, 50 x max observed c_j], one

expand-and-retry, per-HH convergence flag. Synthetic unit tests with closed-form

known solutions for all three modes (construct toy utilities where w* is analytic).

Record from the data and STATE in the report: the consumption unit (monthly vs

annual real-2016 EUR) and exactly where c_scale enters, so u(w, .) uses the

identical scaling as the estimation-side index. w is expressed in those same units.

TASK 1 - W3 RE-VALIDATION ON THE CONFERENCE STEM (validation only, never reported).

Smoke 25 HH/group, then all 5,007 singles. Gates:

 (i) consistency identity: at w=0 the solver's reference-side inclusive value must

     equal the stored V_i_IS_staged per household to <= 1e-9 (same extractor

     conventions - this ties F3-R and F4-R together falsifiably);

 (ii) Omega ~ 0: |Omega_W3| <= 1e-8 for all converged HH (Stage-1 achieved ~3e-10);

 (iii) non-convergent < 0.5% per group.

STOP on any failure; report, do not proceed to real measures.

TASK 2 - W4 STAYING-HOME EQUIVALENT (first real measure).

V_ref(w) = u(w, l_home) with the household's own preference covariates and group

parameters; solve u(w, l_home) = V_actual. Implement BOTH the numerical solve and

the ANALYTIC inversion of the consumption term (Box-Cox algebra); agreement <= 1e-8

per household is the gate. Report bracket failures by DIRECTION: below-floor

(w* would be < 1.0 - economically meaningful, count and list separately) vs

above-cap. Production over all singles.

TASK 3 - W1 EQUAL-PAY OVER THE OWN SET.

B(w) = own latent set (same draws, same log g-hat - log pi weights as V_actual)

with consumption REPLACED by w at ALL nodes including home:

V_ref(w) = logsumexp_j [ u(w, l_j) + log g_j - log pi_j ]. Unit test first

(kappa-recovery): synthetically set all c_j := kappa for one household, the solver

must return w* = kappa to <= 1e-8. Then production over all singles. Also compute

the pre-registered sensitivity variant (replacement at working nodes only, home

keeps actual non-employment consumption) into a SEPARATE column - appendix only,

never a headline.

TASK 4 - W6 EQUAL-PAY OVER THE UNIVERSAL GRID.

Read the canonical hours bands from the certified estimation spec and RECORD the

grid (band values + home node) in the report BEFORE any Omega_W6 is computed.

J = {home} + {one node per canonical hours band}, identical for every household;

V_ref(w) = logsumexp_{j in J} u(w, l_j) with UNIFORM weights (no g-hat, no pi),

own preference covariates. Gates: kappa test on J; duplicate-household invariance

(clone a household: identical preference covariates + identical V_actual must yield

bit-identical w*). Production over all singles.

TASK 5 - OUTPUTS + PROVENANCE.

outputs/welfare/fastlane/singles_measures_omega_v1.parquet with per-HH columns:

uid, group, year_tag, V_actual, Omega_W3, Omega_W4, Omega_W1, Omega_W1_workonly,

Omega_W6, per-measure convergence flags + bracket info. Provenance block: theta

hash, spec hash, stem, c_scale, consumed F3-R parquet content hash, solver

version/commit, recorded W6 grid.

Report: docs/jmp_methodology/RURO_welfare_singles_measures_report_v1.md - per-gate

verdicts, the W6 grid, units statement, non-convergence counts, and an Omega sanity

readout LIMITED to min/median/max per measure per group (no dispersion or

inequality statistics). End with explicit READY/NOT-READY for F5. Do not commit.

Report paths.

```

Firing logic, so there's no ambiguity while you juggle sessions: **F3-R now**. When its report lands, the `READY FOR F4: yes` line is mechanical (it depends only on the Task-1 negLL gate and the Task-3a anchor verification), so you may fire **F4-R immediately** on that line and paste the report here in parallel — my review adds value on the b1/b2 and parity sides, which gate **F3-B**, not F4-R. If Task 4 says `LICENSED`, F3-B runs alongside F4-R without blocking anything; if `NOT LICENSED`, the stratified target-only fallback does the same job by Day 9.
