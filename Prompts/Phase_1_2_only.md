Done and validated — stopping here without committing, as instructed.

What was added (the only two files modified, verified by git):

fr_singles_pipeline_v2.ipynb (monorepo, +127/−1, edited via nbformat only, 59 → 60 cells):

Controls cell now carries a sixth flag, EXPORT_PRODUCTION_GEOMETRY = False, included in the printed controls dict; the five original run flags are untouched and still default False.
One new code cell inserted at index 32 — immediately after the draws_p2a construction-and-gates cell, before pricing §10. Its entire body is a single top-level if EXPORT_PRODUCTION_GEOMETRY:; with the flag False it only prints [SKIPPED: EXPORT_PRODUCTION_GEOMETRY=False] production geometry freeze not run.. When True it: requires the in-memory draws_p2a; loads the contract from scripts/p2a/configs/p2a_regionlive_rebuild_v1.yaml; preserves the full column set; mergesorts by (idhh, draw) and resets the index; validates 157,055 rows / 1,555 households / 101 alternatives each, required columns, one draw==0 chosen row per household with is_chosen==1 and log_prior==0, no duplicate keys, seed 2026 (config check plus child-seed reproduction from default_rng(2026) — verification only, no draw generation), prior > 0, finite log_prior, and the Wave-0.1 identity at exact equality; then writes atomically (temp parquet → SHA-256 of the completed file → contract re-validated on the on-disk bytes → os.replace) to region_live_v1/inputs/ only, with the full metadata JSON. Existing-file rule implemented exactly: create only if neither exists; both-valid → "already frozen", no rewrite; one-only or contract-fail → raise, no overwrite.
Banner documents the sole authorized production-output exception (operator-gated, region_live_v1/inputs/ only, authorizes nothing else).
FR_P2a_region_live_notebook_integration_addendum_v1.md (MNL, +55/−3): §5 output-isolation amended with the single exception, §14 item 1 updated (cell now exists; destination superseded to region_live_v1/inputs/ per plan v2 §8; execution still pending), and a new §15 amendment recording the freeze-cell contract and validation.

Static validation — ALL 15 CHECKS PASS (no execution): v1 hash byte-identical (61b9cf4d…, also clean per git); v2 nbformat-parses and schema-validates; all 36 code cells AST-compile; every execution_count is None with empty outputs; the flag exists and defaults False; exactly one code cell references region_live_v1 (the freeze cell, targeting inputs/ only — I reworded a controls-cell comment so the literal path appears nowhere else); the freeze cell is a single if on the flag whose False-branch only prints the skip message; no draw-generation function and no engine-ready read inside it; placement verified on both sides.

Git diff summary: MNL — docs/France_case/P2a/FR_P2a_region_live_notebook_integration_addendum_v1.md (+55/−3) plus the dclaborsupply-monorepo gitlink marker; monorepo — notebooks/fr_singles_pipeline_v2.ipynb (+127/−1) only; fr_singles_pipeline_v1.ipynb unchanged. Nothing committed.

The G-0 unblock path is now: an operator sets EXPORT_PRODUCTION_GEOMETRY=True, runs v2 through §9 and the freeze cell (pricing stays cached; estimation off), then re-runs the Phase 1–2 dry-run command — G-0 will then admit Phase 1.