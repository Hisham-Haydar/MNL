# Output Runs to Keep

The project-level active results registry is:

```text
docs/estimation/RURO_ACTIVE_RESULTS_REGISTRY.md
```

This local file exists so that anyone browsing `outputs/` can find the registry. The `outputs/` directory is ignored by git, so the tracked documentation should live in `docs/`.

