# ==============================================================================
# RURO MNL Estimation Specification - Ultra Minimal (Log-Only)
# ==============================================================================
# Maximum simplification:
# - Log utility only (no Box-Cox curvature parameters)
# - Pooled consumption (single beta_c for all groups)
# - Pooled leisure by gender only (beta_l0_m for males, beta_l0_f for females)
# - No preference shifters
# - Only 7 parameters total
# ==============================================================================

specification:
  name: "ultra_minimal_log"
  description: "Ultra-minimal spec: log utility, pooled consumption and leisure by gender (7 params total)"
  wage_spec: "vw"

# Utility Function (Preference Side)
utility:
  functional_form: "log"  # Log utility only - NO Box-Cox curvature

  consumption:
    coefficient: "beta_c"  # POOLED across all demographic groups
    box_cox_exponent: null

  leisure:
    intercept: "beta_l0"  # Will be split by gender: beta_l0_m, beta_l0_f
    box_cox_exponent: null
    shifters: []

# Hours Opportunity Density
hours_opportunity:
  shifters:
    - variable: "working"
      coefficient: "beta_work"
      description: "Working indicator (h>0)"

# Wage Opportunity Density
wage_opportunity:
  specification: "log_normal"

  mean_shifters:
    - variable: "intercept"
      coefficient: "beta_w0"
      description: "Log-wage intercept"

  variance:
    parameter: "sigma"
    description: "Standard deviation of log-wage residual"
    bounds: [0.1, 20.0]

# Singles/Couples sections
singles:
  gender_specific_leisure: true

couples:
  gender_specific_leisure: true
  leisure_interaction:
    coefficient: null

# Parameter Initialization
# ==============================================================================
# IMPORTANT: Must provide initial values for ALL parameters the parser will create,
# including group-specific ones (parser creates them based on gender_specific_leisure: true)
#
# Parameters created by parser:
# 1. beta_c         - consumption utility (pooled across all)
# 2. beta_l0_sm     - leisure intercept for single males
# 3. beta_l0_sf     - leisure intercept for single females
# 4. beta_l0_m      - leisure intercept for couple males
# 5. beta_l0_f      - leisure intercept for couple females
# 6. beta_work      - hours opportunity (working indicator)
# 7. beta_w0        - wage intercept
# 8. sigma          - wage variance
# ==============================================================================
initial_values:
  # CONSUMPTION: Pooled across all groups
  beta_c: 1.1  # Average of: beta_c (1.2740), beta_c_sm (1.0399), beta_c_sf (0.7602)

  # LEISURE: Pooled by gender (but parser still creates group-specific names!)
  # We provide the same value for sm/m (males) and sf/f (females)
  beta_l0_sm: 0.35  # Single male - pooled with couple male. Average: (0.4179 + 0.2886) / 2
  beta_l0_sf: 1.01  # Single female - pooled with couple female. Average: (0.6778 + 1.3515) / 2
  beta_l0_m: 0.35   # Couple male - same as single male
  beta_l0_f: 1.01   # Couple female - same as single female

  # OPPORTUNITY & WAGE
  beta_work: 1.3950
  beta_w0: 2.1850
  sigma: 0.4723

# Optimization Settings
optimization:
  method: "L-BFGS-B"
  analytical_gradient: true
  max_iterations: 10000
  tolerance: 1.0e-9
  gradient_tolerance: 1.0e-6
  disp: true
  iprint: 1

# No bounds needed (log utility, all params are free)
