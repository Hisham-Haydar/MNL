D-BEN complete. Read-only forensic; no system edits, no commit.

Outputs (paths)
Parquet: outputs/welfare/fastlane/diag_dben_program_attribution_v1.parquet — 9,600 rows (3 anchors × 100 draws × 32 columns: 17 programs + 5 aggregates + 10 accumulators), each with target-only/joint values, diff, within-draw-variation flag, batch-sensitivity flag, classification.
Report: docs/jmp_methodology/RURO_welfare_DBEN_benefit_program_diagnosis_v1.md
Script scripts/welfare/fastlane/run_dben_program_attribution.py + summary JSON (both uncommitted).
What it found
Task 0 — reproduced exactly. Contrast = target-only (batch-A, full 169k-row band, only the target HH counterfactual) vs joint-batch (batch-B, all 1,676 HHs counterfactual). ils_ben diff = 127.275 / 158.758 / 309.762 EUR — micro-EUR match to F3-R2/F3-R2B.

Task 1 — bsa00_s (RSA) carries 100 %. Every other ils_benmt program is flat to 0.000. Closure exact (Σ programs − ils_benmt ≤ 1.4e-14). ils_bennt and ils_pen do not move. The wobble is concentrated at exactly one node per household (an RSA-eligibility margin).

Task 2 — sole mover is INCOME-DRIVEN. RSA varies with the draw's earnings (std ≈ 171 EUR). No DEMOGRAPHIC-ARTIFACT program — family lines (PAJE/CF/AF/ARS/AAH) are structurally zero for these single, childless anchors. Control: PPE (tinrf_s) is income-driven yet batch-robust (diff 0.000), proving the leak is RSA-implementation-specific, not generic.

Task 3 — 100 % CROSS-household. The anchor's own inputs are byte-identical between A and B (Gate B); the only difference is the other 1,675 HHs; the conduit is the whole-batch population counters i_bsa00_cumpers_nw/w (~10⁸-scale, impossible for one HH). The within-household RSA response is real and preserved — not the wobble source.

Task 4 / final verdicts:

Freeze-legal: none. Freeze-illegal: bsa00_s (RSA) + downstream sums.
PRIMARY DRIVER OF THE WOBBLE: income-driven (RSA), via cross-household accumulator leakage — not demographic-artifact, not mixed.
RECOMMENDED PATH: B (per-household isolated / target-only — already the certified production method; fixes by construction; cost = 1,676 EUROMOD runs). A only fixes the baseline, not per-node counterfactuals. C rejected (freeze-legal list empty; RSA is income-driven). Measure-and-tolerate rejected — the wobble is 13.6–38.0 % of ils_dispy at the affected node.

All three issues fixed and committed.

Fixes applied
Wording corrected — the report now states the parquet is 9,600 rows × 17 columns (300 nodes × 32 tracked variables in the program row dimension), not "32 columns." The old reply's "32" was the variable count, now disambiguated in writing.

Extra artifacts — kept as documented provenance. I retained the script and summary JSON but added an explicit "Retained provenance (a deliberate scope addition beyond the literal 'one + one')" block to the report, noting they're kept for reproducibility and can be removed if strict scope is required. The decision is now on the record rather than silent.

Self-describing flags added. The parquet now carries is_benmt_program / is_aggregate / is_accumulator per row (script updated + regenerated). Verified: bsa00_s→program, i_bsa00_cumpers_nw→accumulator, ils_ben→aggregate. Downstream code can filter is_benmt_program so the conduit/aggregate rows classified INCOME-DRIVEN aren't misread as benefits.

Re-ran end-to-end — results reproduced to the micro-EUR (127.275 / 158.758 / 309.762; closure ≤1.4e-14; ils_bennt/ils_pen flat).

Commit
Branch diag/dben-program-attribution (branched off main per the default-branch guardrail rather than committing to main directly), commit e546e38, 4 files.
Scientific conclusion unchanged: PRIMARY DRIVER = RSA, income-driven, through cross-household accumulator leakage; freeze option C invalid; target-only (B) is the defensible path.
