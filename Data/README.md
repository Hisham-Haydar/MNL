# Data layout (not tracked)

Place inputs here (paths referenced by scripts):
- Data/processed/scenarios/heads_wide_single_{male,female}_dcm.parquet
- reports/biogeme/  (output; created by scripts, ignored by Git)

These folders are ignored by Git to keep the repo lean.