he F6-PRICE-B preflight is complete. Here is the summary:

PREFLIGHT GATE: FAIL — Task 0 complete, Task 1 blocked on two independent issues.

Task 0(a) — Canonicalization (DIAGNOSTIC): 97 stale lhw/yem rows across 13 preflight HHs in non-employed draws. Would be zeroed by _overwrite_fixed before Task 1 pricing.

Task 0(b)+(c) — Identity gate: STRUCTURAL FAIL

The gate fails for a fundamental reason, not a bug. The bpool priced parquet was built using draw-first chunk EUROMOD calls (all 1,676 HHs at a draw-range together, ~84k rows per call, accumulator resets between calls). The F6 preflight uses a uid-first full-band EUROMOD call (all 101 draws for uid1, then uid2, …, single 169k-row call, accumulator runs continuously). The FR RSA accumulator (i_bsa00_cumpers_nw/w, the sole batch-sensitive channel proved by D-BEN) sees a much larger prior-row count at each HH position in uid-first ordering. This geometry difference — not any input value error — drives bsa00_s from 0 (stored bpool) to 526 EUR (uid-first run) at ALL 100 draws for ALL 13 preflight HHs, regardless of whether correct yem00/yemxp are provided. Not fixable by input correction.

Welfare-consistency implication: F4C/F5 actual baselines (V_i^IS, ils_dispy_real) used draw-first geometry → bsa00_s=0 for RSA-eligible HHs. Any uid-first counterfactual pricing would give bsa00_s=526, inflating welfare changes by ~526 EUR/month as an artefact. Operator must choose: (A) F6 uses draw-first chunk geometry (consistent with F4C/F5), or (B) recompute F4C/F5 baseline in uid-first geometry.

Two blockers for Task 1:

Identity gate FAIL — geometry design decision needed before a valid pricing path exists
JMP_decomposition_design_memo_v1.md not on disk — F6 IMPLEMENTATION AUTHORIZED: NO (commit bde5085)
READY FOR F6-RUN ACCESS OPERATOR: NO


Markdown lint warnings in the memory file — minor style issues only, not fixing since it's a private index file.

The geometry audit is complete. RURO_welfare_F6PRICEB0_geometry_audit_v1.md documents three findings:

Task 1 (COMPLETE): The stored 2016 singles bpool is a 241,895-row, single EUROMOD call, UID-FIRST + DRAW-SECOND + PERSON-THIRD — 169,276 decider rows plus 72,619 non-decider rows (children). The Two-M bug means every column in the stored c0 except the 5 headline cols (ils_dispy, ils_origy, ils_ben, ils_tax, ils_sicdy) is a stale precompute carry-over, including bsa00_s.

Task 2 (FAIL): Reproduction gives max abs diff of 184.6123 EUR at 3,460 rows / 372 HHs on ils_dispy. Root cause: FR.xml and FR_DataConfig.xml were updated 2026-05-26 09:30 UTC, 35 hours after the chunk was created 2026-05-24 23:21 UTC. Inputs are byte-identical; the EUROMOD FR_2015 formula changed (+15 output columns in the new system).

Task 4 — F6 geometry: BLOCKED. Three independent blockers now on record:

EUROMOD system version mismatch — F4C/F5 baseline and any new F6 run use different FR_2015 versions
Batch geometry incompatibility — uid-first full-band gives bsa00_s=526 vs chunk's 0 (from preflight)
Equalized covariate spec not ratified (from governance, commit bde5085)
