I have the files now, and reading the actual source changes the F6 design in two concrete ways that I could only flag speculatively before. Both are now verified against the code, not guessed. Here's what the source tells me, then the corrected memo prompt with the exact attach list.

**Verified finding 1 — the equalization mechanism the welfare layer already supports is *covariate substitution*, and the extractor closes over covariates as constants.** In `welfare_core.py`, `_build_V_extractor` captures `leisure`, `working`, `log_wage`, and the market/occupation covariate arrays as fixed `jnp` constants at build time; only `theta` and `consumption_arr` are call arguments (that's the W³ shift path). So equalizing access or ability means rebuilding V with *different covariate arrays* — which is a real (small) extension, exactly the "surface as a finding, don't silently do" item. The cut maps cleanly: the spec confirms `educL/educH/pexp_years/pexp_years2` feed only `wage_opportunity.mean_shifters` (→ ability via the offered-wage μ), and `gsur/reg2-8/drgur/drgmd/year_*` plus `loc4_*` feed `market_opportunity`/`occupation_opportunity` (→ access). The `welfare_stage1_w3.yaml` `blocks:` section *already encodes this exact partition* (preference/ability/access lists) — so F6 reads the block membership from config, doesn't reinvent it.

**Verified finding 2 — the ability-equalization has a subtlety the wage block forces, and it is the single most important thing for F6 to get right.** Look at the `log_w` term: `resid = (log_wage − μ)/σ`, `log_w = −0.5 resid² − log σ − 0.5 log2π − log_wage`. The offered wage enters V through *two* channels — the density `log_w` (how probable this wage is, given education/experience) **and** consumption (the wage, post-EUROMOD, is what funds `c`). Equalizing education/experience covariates shifts μ, which changes `log_w` (the offer-density channel) — but `log_wage` itself and the consumption `c` are *data*, frozen on the actual side. So covariate-only ability equalization moves the *density* channel but not the realized-wage→consumption channel. That is a genuine design decision the memo must rule on explicitly and cannot paper over: does "equalize ability" mean (a) equalize only the wage-*offer density* (set μ to reference; cheap, no EUROMOD, internally consistent with W¹/W⁴/W⁶ which are all consumption-direct), or (b) also re-draw/re-price offered wages at reference productivity (which re-opens EUROMOD on counterfactual nodes — the expensive path you deprioritized everywhere else). **The whole consumption-direct architecture of your conference path implies (a).** The memo must state (a) as primary, name what it does and doesn't capture, and flag (b) as a post-conference extension — otherwise an agent will silently pick one.

These two findings are *why* the memo is Opus-high-and-verify-against-source rather than a quick write-up: the ability channel is where a from-memory memo would have gone wrong.

Everything else from last turn holds and is now confirmed against the docs: **Design A** (two equalizable factors {access, ability} + preference as the non-equalized residual) is exactly what `JMP_welfare_measurement_decisions_memo_v2.md` §16 commits to ("preference-neutralised," explicitly *not* equalized — collapsing preferences is "alien to the JMP's preference-respecting design"); **W¹ is the decomposition headline** (noise-robust per the F4 cancellation finding); **reference = type-conditional medians** (§8); **[access-only, access+ability] bracket** is the deliverable; **σ in the broad ability bound with an excluded-σ sensitivity** (the ability-cut memo's explicit caveat). One reconciliation the memo must note: the ability-cut memo §5 describes a 3! Shapley, while decisions-memo §16/§18 describe two-way-with-residual — these resolve as A-primary (two-way + residual), full three-way as the robustness appendix.

**Files to attach for the F6 memo session** (you've now made these visible — confirm all present):

The four load-bearing, which I've now read and which the prompt cites by `file:line`: `scripts/welfare/welfare_core.py`, `scripts/bpool/jax_ll_probe.py`, `scripts/bpool/specs/estimation_spec_joint_pooled_v1_bll0_tlmpin.yaml`, `scripts/bpool/specs/theta_hat_realdata_901_v1.csv`. The governance the memo aligns to: `docs/France_case/_shared/governance/JMP_ability_opportunity_cut_v1.md`, `docs/jmp_methodology/JMP_welfare_measurement_decisions_memo_v2.md`, `docs/jmp_methodology/JMP_measure_mapping_memo_v1.md`. The config that already encodes the block partition: `scripts/welfare/configs/welfare_stage1_w3.yaml`. The result chain: `RURO_welfare_F4C_final_singles_measures_report_v1.md`, `RURO_welfare_singles_measure_family_F5_report_v1.md`, `RURO_welfare_F5_primary_scope_ratification_v1.md`. **Do not attach** the EUROMOD microdata, the staged `.parquet` stems, or the large inventory CSVs — the memo reasons about parameters and covariates, never household rows.

Here is the corrected F6 design-memo prompt (replaces the one from last turn; the two verified findings are now baked into Sections 2–3, and the citations point at the real lines):

```
F6 DECOMPOSITION DESIGN MEMO (DESIGN ONLY — no code, no decomposition run, no
estimation, no commit). Produce ONE markdown file and VERIFY every structural
claim against the named source by file:line. Platform: Claude Code, Opus, high,
thinking ON. Output:
  docs/jmp_methodology/JMP_decomposition_design_memo_v1.md

PURPOSE
Pre-register the equalization scheme that turns the F5 welfare distributions into
the opportunity/ability/preference decomposition answering the main question. This
memo precedes and governs the F6-RUN implementation, as F2 governed F4. It is the
prose-design-before-code gate; F6-RUN code does not start until this is signed off.

VERIFY-AGAINST (cite file:line; trust the code over prior prose):
- scripts/welfare/welfare_core.py — _build_V_extractor (the covariates captured as
  constants vs theta/consumption_arr as args), w3_inversion (the shift-mode solver),
  compute_group_welfare, gate0_parity, the V = u+log_h+log_w+log_market-log_prior line.
- scripts/bpool/jax_ll_probe.py — the same V construction in build_jax_singles_ll;
  confirm exactly which named params/covariates enter each block.
- scripts/bpool/specs/estimation_spec_joint_pooled_v1_bll0_tlmpin.yaml — wage_opportunity
  mean_shifters (educL/educH/pexp_years/pexp_years2 -> mu; sigma=variance),
  market_opportunity (gsur/reg2-8/drgur/drgmd/year_*), occupation_opportunity (loc4_*).
- scripts/welfare/configs/welfare_stage1_w3.yaml — the blocks: preference/ability/access
  lists ALREADY encode the cut; read block membership from here, do not reinvent it.
- docs/France_case/_shared/governance/JMP_ability_opportunity_cut_v1.md — the normative cut.
- docs/jmp_methodology/JMP_welfare_measurement_decisions_memo_v2.md §§8,14-19 (reference,
  counterfactuals, index, rule, interactions) — especially §16 (preference NEUTRALISED, not equalised).
- docs/jmp_methodology/JMP_measure_mapping_memo_v1.md and
  docs/jmp_methodology/RURO_welfare_F5_primary_scope_ratification_v1.md (scope + inference contract).

SECTION 1 — THE CUT, MAPPED TO NAMED PARAMS, COVARIATES, AND V-TERMS (verified).
Table: component -> spec block -> coefficient names -> covariate arrays in
_build_V_extractor -> which V term they enter (log_w via mu, log_market, log_h, u).
  ACCESS: hours (beta_E, beta_h_pt1/pt2/ft/lh) + market (beta_E_gsur, beta_E_drgn2..8,
    beta_E_drgur, beta_E_drgmd, beta_E_y2015/y2017) + occupation (beta_occ_2/3/4_{m,f}).
  ABILITY: wage block (beta_w0 anchor, beta_w_educL/educH/pexp/pexp2, sigma).
  PREFERENCE: leisure blocks, theta_l_*, theta_c_singles, beta_ll, beta_c numeraire.
Confirm the welfare_stage1_w3.yaml blocks: lists match this exactly; cite the line.

SECTION 2 — EQUALIZATION MECHANISM (the load-bearing verified finding).
State plainly, from the code: _build_V_extractor captures the covariate arrays
(leisure, working, log_wage, market/occ covariates) as CONSTANTS; only theta and
consumption_arr are call args. Therefore equalization = rebuilding V with reference
covariate arrays, and F6-RUN needs a covariate-override extension of the extractor
(a FINDING to surface, with a parity gate: override=actual reproduces F5 V_actual to
<=1e-9). Define each operation as a covariate substitution at FIXED theta:
  ACCESS-equalized: set gsur, reg dummies, drgur/drgmd, year, occ covariates to
    type-conditional (sm/sf) reference (median/modal); recompute log_market, log_h,
    log_w-occ-independent. Name every array changed.
  ABILITY-equalized: set educL/educH/pexp_years/pexp_years2 to type-conditional
    medians. CRITICAL RULING (state and justify): the offered wage enters V via TWO
    channels — the offer DENSITY log_w (through mu) AND consumption (the realized wage
    funds c, which is frozen EUROMOD data). Primary design = (a) equalize the wage-
    OFFER-DENSITY channel only: set mu to reference via the education/experience
    covariates, leaving realized consumption as data — consumption-direct, NO EUROMOD,
    internally consistent with W1/W4/W6. Flag (b) re-pricing offered wages at reference
    productivity (re-opens EUROMOD on counterfactual nodes) as a POST-CONFERENCE
    extension, NOT the conference path. sigma: include in the broad ability bound WITH
    the excluded-sigma sensitivity (ability-cut memo residual-heterogeneity caveat).
  PREFERENCE: NOT equalized (Section 3).
Each operation holds the other blocks' covariates and theta fixed (decisions-memo
§14: "structural parameter vector unchanged; only the covariate inputs are modified").

SECTION 3 — DECOMPOSITION DESIGN (recommend A; reconcile the two source memos).
DESIGN A (PRIMARY, per decisions-memo §16): two equalizable factors {access, ability}
  + preference as the NON-equalized residual. Shapley over {access, ability};
  preference = I(actual) - I(access+ability jointly equalized). Opportunity share =
  bracket [access-only Shapley share, access+ability joint share]; preference =
  residual. No reference-preference; the §16 yardstick problem is avoided.
DESIGN B (ROBUSTNESS APPENDIX ONLY): full 3! Shapley over {access, ability, preference}
  with a reference-preference (pooled or reference type) + reference-type sensitivity.
  This is what the ability-cut memo §5 sketches; reconcile by stating A is primary
  (it is what decisions-memo §16 commits to) and B is the robustness exposure, never
  headline. Justify from §16 verbatim.

SECTION 4 — SHAPLEY ARITHMETIC + INDEX (verified).
Design A: enumerate the 2! orderings over {access, ability}; each factor's Shapley
contribution = mean marginal inequality reduction across orderings; preference =
I(actual) - I(both-equalized). Secondary: ordered-removal; report the access-ability
interaction as |Shapley - ordered| (decisions-memo §18-19). Index = survey-weighted
Gini (the validated F5 primitives, weight dwt); primary measure W1; W4/W6 robustness;
on the ratified 2016 singles scope (year_tag==2, n=1,676).

SECTION 5 — REFERENCE VALUES (type-conditional, pre-registered).
Type-conditional (sm/sf) reference: median gsur, modal region (reg1 ref, reg2-8=0),
modal urbanisation, year=2016, median educL/educH/pexp/pexp2. Sensitivity =
best-available/maximal (appendix). The RULE is pre-registered here; the exact
median/modal VALUES are read from the 2016 data by F6-RUN and printed in its report
BEFORE any decomposition number.

SECTION 6 — INFERENCE (defer; state contract). Point decomposition first. CIs = the
F6-BOOT cluster bootstrap (B=200, resample 9,657 joint idorighh, re-estimate theta per
replicate, recompute the WHOLE decomposition per replicate on 2016 singles). The
decomposition, not just the Ginis, is recomputed inside each replicate. No bootstrap
run by this memo.

SECTION 7 — F6-RUN DELIVERABLES (gate list, build order).
  0. Covariate-override extractor extension; PARITY GATE override=actual reproduces
     F5 V_actual/Omega to <=1e-9 per HH (ties to F4C/F5). STOP on fail.
  1. ACCESS-equalized Omega (W1); report inequality change (no assumed direction).
  2. ABILITY-equalized Omega (offer-density channel, design (a)); access+ability JOINT.
  3. Shapley {access,ability} + preference residual; ordered-removal diagnostic; the
     [access-only, access+ability] opportunity bracket + preference residual.
  4. W4/W6 robustness; excluded-sigma sensitivity on the broad bound.
  5. Per-HH counterfactual Omega parquet + decomposition-table parquet; full provenance
     (theta/spec hash, stem, reference values, extractor/solver version). READY/NOT-READY
     for F6-BOOT + F7.

SECTION 8 — SIGN-OFF CHECKLIST (operator reply in chat):
  - Design A (two-factor {access,ability} + preference residual) primary, B robustness — confirm/override
  - W1 decomposition headline, W4/W6 robustness — confirm
  - Ability = wage-OFFER-DENSITY channel only (design (a), consumption-direct), (b) deferred — confirm
  - Type-conditional median reference (best-available sensitivity) — confirm
  - sigma in broad ability bound WITH excluded-sigma sensitivity — confirm

End with: REQUIRED NEXT INPUT = operator sign-off on Section 8, after which F6-RUN is
written against this memo. Do not commit. Report the path.
```

**What to do with the reply:** paste the memo. I'll check three things against the source I've now read — Section 1's param/covariate/V-term mapping against `jax_ll_probe.py` and the YAML `blocks:`, Section 2's ability ruling (that it commits to design (a), offer-density-only, and names what that does *not* capture), and Section 3's A-vs-B reconciliation — then write **F6-RUN**. That run produces the opportunity bracket X–Y and the preference residual Z: your headline.

Two flags, same as last turn but now sharper. The **ability ruling in Section 2 is the genuinely consequential modeling choice** — design (a) is right for the conference (consumption-direct, matches your whole architecture) but it equalizes the *offer-density* channel only, so the memo must be explicit that the realized-wage→consumption channel of ability is held at actual; that's defensible and standard but must be stated, not buried, or a referee will find it. And **F6-BOOT (200-replicate re-estimation) remains the binding 15-day risk** — independent of this memo, embarrassingly parallel, should be launching now while you review; Section 6 just records its contract. Want me to finalize the F6-BOOT prompt alongside so both run in parallel?
