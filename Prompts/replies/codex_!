Do **not** run it as-is. It pre-ratifies several choices that the source does not support.

**Blocking issues**

1. **Design A is not established as primary.**  
   `JMP_welfare_measurement_decisions_memo_v2.md:819-855` specifies primary Shapley-Shorrocks and explicitly describes the three-way `3!` decomposition. Section 16 defines preference-neutralisation, but does not ratify two-factor Design A as primary.

2. **The access operator is incomplete.**  
   The proposed substitutions change market covariates, but cannot claim to equalize `log_h`. `log_h` uses alternative-state arrays such as `working_pt1` and `working_ft` at `welfare_core.py:194-202,253-256`. An hours-access operator remains undefined.

3. **The sigma operation is undefined.**  
   `sigma` is one common parameter, not a household covariate. Fixed-theta covariate substitution cannot equalize residual productivity through sigma. Changing common sigma also does not eliminate household residual-wage differences.

4. **Reference construction can generate invalid states.**  
   Elementwise medians of `educL` and `educH` may not represent a coherent education category. Likewise, “modal region” cannot automatically mean `reg1`; the modal category must be computed and then one-hot encoded.

5. **Gender attribution is missing.**  
   Type-conditional `sm`/`sf` references may preserve gender-based occupation-access differences that governance classifies as access. The applicability of legacy A1/A2/A3 rules to the current pooled spec must be audited.

6. **W1 cannot silently become the sole decomposition headline.**  
   The roadmap pre-registers W1/W4/W6 as headline measures. W1 can be the first implementation/gate, but making it primary requires ratification.

Also, do **not** launch F6-BOOT yet. It cannot recompute a decomposition whose counterfactual operators remain undefined.

Use this corrected prompt:

```text
F6-DESIGN-R: DECOMPOSITION OPERATOR AND GOVERNANCE RECONCILIATION MEMO.

DESIGN ONLY. No code, decomposition calculations, estimation, bootstrap,
generated-artifact edits, or commits. Create exactly one markdown file:

docs/jmp_methodology/JMP_decomposition_design_memo_v1.md

PURPOSE

Define and pre-register implementable access, ability, and preference
counterfactual operators for the certified pooled specification. Surface every
unresolved normative or implementation choice. Do not pre-ratify a design where
the governing sources conflict.

VERIFY AGAINST SOURCE WITH file:line CITATIONS

- scripts/welfare/welfare_core.py
- scripts/bpool/jax_ll_probe.py
- scripts/bpool/specs/estimation_spec_joint_pooled_v1_bll0_tlmpin.yaml
- scripts/welfare/configs/welfare_stage1_w3.yaml
- docs/France_case/_shared/governance/JMP_ability_opportunity_cut_v1.md
- docs/jmp_methodology/JMP_welfare_measurement_decisions_memo_v2.md
- docs/jmp_methodology/JMP_measure_mapping_memo_v1.md
- docs/jmp_methodology/RURO_welfare_F5_primary_scope_ratification_v1.md
- docs/JMP_results_campaign_roadmap_v1.md

RATIFIED INPUTS

- Evaluation scope: 2016 singles, year_tag == 2, n = 1,676.
- Parameters: certified pooled theta-hat.
- Weights: dwt.
- Measures: W1, W4, and W6 are headline measures; W3 is validation-only.
- Inference contract: B=200 replicates resampling the full joint estimation
  sample's 9,657 idorighh clusters, re-estimating theta and recomputing the
  finalized decomposition.
- F3 joint-batch redraw pricing is NOT LICENSED.
- Proposal correction terms are integration machinery, not substantive factors.

SECTION 1 - CURRENT-SPEC FACTOR MAP

Produce an exhaustive table containing:

- coefficient;
- source covariate or alternative-state array;
- V term entered: u, log_h, log_w, log_market, or log_prior;
- proposed factor: access, ability, preference, fixed structural term, or
  integration machinery;
- whether variation is household-level, alternative-level, group-level, or
  common;
- whether a valid equalization operator currently exists.

Read block membership from welfare_stage1_w3.yaml, but state explicitly that its
blocks are classification/decomposition-readiness metadata, not an operational
equalization algorithm.

Identify legacy governance terms that do not exist in the current certified
pooled specification, including any obsolete gender-on-arrival or gender-on-wage
rules.

SECTION 2 - EXTRACTOR EXTENSION FINDING

Verify that _build_V_extractor captures covariate and alternative-state arrays
as constants while accepting only theta and consumption_arr at call time.

Specify the smallest required F6-RUN extension: a validated covariate/state
override interface.

Required future parity gate:

override=actual must reproduce every household's frozen F4C/F5 actual welfare
and aggregate inequality to <= 1e-9.

Do not implement it here.

SECTION 3 - ACCESS OPERATOR FEASIBILITY

Treat the access block as separate subchannels:

1. Household market covariates:
   gsur, region, urbanisation, and year indicators.

2. Occupation alternative-state attributes:
   loc4 and loc4_*.

3. Hours alternative-state attributes:
   working and working_pt1/pt2/ft/lh.

For each subchannel state:

- what varies across households;
- what would be replaced;
- whether replacement preserves valid categorical/state structure;
- whether it changes job identity or only density weights;
- whether fixed existing nodes are sufficient;
- whether redraws, common grids, or pricing would be required.

Do not claim access equalization recomputes or equalizes log_h unless a valid
operator for working/hour-band arrays is defined.

Do not treat elementwise replacement of loc4 dummies as automatically equivalent
to a common occupation-offer distribution.

Conclude whether a complete access operator is currently implementable. If only
the household-market subchannel is implementable, label it explicitly as a
partial-access counterfactual, not the full access contribution.

SECTION 4 - ABILITY OPERATOR FEASIBILITY

Separate three distinct ability concepts:

A. Wage-offer-density mean channel:
   replace coherent education and potential-experience covariates, changing mu
   while holding observed log_wage and consumption fixed.

B. Realized-wage/consumption channel:
   counterfactually change wages and reprice consumption through EUROMOD.

C. Residual-productivity/sigma channel.

For A, state explicitly that it equalizes only the wage-offer-density channel.
It does not equalize realized wages, consumption, or complete productivity.

For B, identify whether it requires unlicensed redraw/repricing machinery and
therefore must be deferred.

For C, state explicitly:

- sigma is one common parameter, not a household covariate;
- fixed-theta covariate substitution cannot equalize sigma;
- changing common sigma does not itself remove household residual-wage
  differences.

Propose candidate residual-productivity operators only as design alternatives.
Do not select one without operator ratification.

Conclude whether the broad access+ability bound is implementable now or blocked
on a residual-productivity ruling.

SECTION 5 - REFERENCE-STATE CONTRACT

Define candidate reference rules without computing values.

Required rules:

- Reference population and weights.
- Household-type and gender treatment.
- Coherent categorical assignment:
  use one modal education category and one-hot encode it; do not independently
  median educL and educH.
- Region:
  compute the modal region and encode it coherently; do not assume reg1 unless
  the computed mode or an explicit ratification selects it.
- Urbanisation, occupation, and year:
  define coherent categorical references.
- Potential experience and continuous covariates:
  define weighted-median rules.
- Gender-access treatment:
  state whether references preserve or equalize gender-segmented occupation
  access and reconcile this with current governance.

List every rule requiring operator ratification.

SECTION 6 - DECOMPOSITION-GOVERNANCE RECONCILIATION

Document the conflict rather than resolving it silently:

- The decisions memo defines preference-neutralised welfare as the residual
  after access and ability equalization.
- The same memo also specifies primary Shapley-Shorrocks and describes a
  three-way 3! decomposition.
- Equalizing preferences to a common reference is explicitly rejected by the
  preference-respecting design.

Present implementable alternatives:

Design A:
two-factor Shapley over access and ability, with
I(both-equalized) reported as the preference residual.

Design B:
full three-way Shapley requiring a separately ratified and implementable
preference intervention.

For each design provide exact equations and accounting identities. Do not label
either design primary until operator sign-off.

The accounting identity for Design A must be:

I(actual)
  = Shapley_access + Shapley_ability + I(both_equalized)

Report the opportunity bracket as:

- lower: access contribution;
- upper: access + ability contribution.

SECTION 7 - MEASURE CONTRACT

Preserve the pre-registered headline family W1/W4/W6.

W1 may be recommended as the first implementation and parity gate, but must not
be silently promoted to the sole headline decomposition measure.

Explain how W4 and W6 pre-compensate opportunity dimensions and how that affects
interpretation of their decomposition results.

SECTION 8 - GATED IMPLEMENTATION PLAN

Design, but do not execute:

- F6-A: override interface and actual-value parity.
- F6-B: bounded tests for each independently valid counterfactual subchannel.
- F6-C: ratified primary 2016 point decomposition.
- F6-D: W1/W4/W6 and reference-rule sensitivities.
- F6-E: B=200 full-joint-cluster bootstrap re-estimation.

Mandatory gates:

- override=actual parity <= 1e-9;
- coherent categorical reference states;
- proposal correction unchanged;
- no unlicensed joint-batch pricing;
- deterministic counterfactual outputs;
- exact Shapley/accounting closure;
- partial operators never labeled as full access or full ability;
- bootstrap not started until all operators and the decomposition design are
  ratified.

SECTION 9 - REQUIRED OPERATOR SIGN-OFF

End with an explicit checklist covering:

- full-access operator, especially hours and occupation;
- density-only versus realized-wage ability treatment;
- residual-productivity/sigma operator;
- gender-access reference rule;
- Design A versus Design B;
- reference-state rules;
- W1/W4/W6 reporting hierarchy.

REQUIRED FINAL LINES

F6 DESIGN STATUS: COMPLETE / INCOMPLETE
FULL ACCESS OPERATOR: DEFINED / PARTIAL / BLOCKED
FULL ABILITY OPERATOR: DEFINED / PARTIAL / BLOCKED
F6 IMPLEMENTATION AUTHORIZED: NO
F6-BOOT AUTHORIZED: NO
REQUIRED NEXT INPUT: operator sign-off on the unresolved checklist

Do not commit. Report the created path and the unresolved decisions.
```

This version lets the memo determine what F6 can defensibly compute instead of forcing a likely partial-access, density-only result to be labeled the full decomposition.
