# JMP Multi-Year Replication 2015 / 2017 — Consolidated v1

**Consolidation date:** 2026-05-25
**Scope:** France case — single-year replication for FR_2015 and FR_2017 (alongside the canonical FR_2016 run), as part of the multi-year stacked-estimation strategy.
**Authoring rule:** structural merge of three pre-existing memos. No content rewritten.

> **Standalone methodology:** the multi-year strategy itself is governed by `docs/jmp_methodology/JMP_multi_year_and_cross_validation_strategy_memo_v3_1.md`. This consolidation covers the **execution chain** for the 2015/2017 single-year runs that feed the stacked estimation.

---

## 1. Sources merged

All three sources are archived at `docs/archive/2026-05-25_docs_supersession/merged_sources/multi_year_2015_2017_command/`. Their original location was `docs/`.

| Original filename | Role |
|---|---|
| `JMP_single_year_replication_2015_2017_authorization_v1.md` | Execution authorization — formal go for running the 2015 and 2017 single-year replications. |
| `JMP_single_year_replication_2015_2017_command_plan_v2.md` | Command plan v2 — step-by-step commands for the replication. |
| `JMP_single_year_replication_2015_2017_command_plan_addendum_v1.md` | Addendum to the command plan — corrections and additions. |

## 2. Chain logic

1. **Authorization** sets the scope and gate conditions for the 2015 and 2017 single-year runs.
2. **Command plan v2** is the operational script for execution.
3. **Addendum** corrects/extends the command plan after issues surfaced.

For the canonical command sequence, read the v2 plan then apply the addendum. For the gate conditions, read the authorization.

## 3. Open items

- Authorial pass: fold the addendum into a clean v3 command plan if the multi-year work continues (out of scope for the 2026-05-25 reorganization).
- Companion `JMP_multi_year_stage_M1_*` execution logs live separately at `docs/France_case/execution_logs/stage_M1/`.

## 4. Manifest reference

See `docs/France_case/cleanup/MOVE_MANIFEST_2026-05-25.md` for source paths, archive paths, and commit SHAs.
